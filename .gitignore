# Runtime session state — never publish. May contain private/proprietary
# walkthrough content (engagement notes, internal architecture, etc.).
cauldron/

# Personal cross-session learning history — never publish.
/ledger.md

# Internal design docs are kept out of the public mirror by convention.
documentation/

# OS / editor noise
.DS_Store
*.swp
